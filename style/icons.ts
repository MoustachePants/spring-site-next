import { Menu, Search, FilterList, Xmark, Map, Clock, SeaWaves } from 'iconoir-react';

const Icons = {
  hamburger: Menu,
  search: Search,
  filter: FilterList,
  close: Xmark,
  map: Map,
  clock: Clock,
  water: SeaWaves,
};

export default Icons;
